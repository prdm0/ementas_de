#logo <- magick::image_read("img/logo.png")
#logo <- magick::image_resize(logo,"48x48!")
#magick::image_write(logo, path = "img/logo_menor.png", format = "png")

#logo_texto <- magick::image_read("img/logo.png")
#logo_texto <- magick::image_resize(logo_texto,"120x72!")
#magick::image_write(logo_texto, path = "img/logo_menor.png", format = "png")